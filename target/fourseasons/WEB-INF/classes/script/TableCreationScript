DROP TABLE IN_FSR_USER_LOGIN;

CREATE TABLE IN_FSR_USER_LOGIN (
"USER_ID" NUMBER(20,0) NOT NULL ENABLE,
"USER_EMAIL" VARCHAR2(40 BYTE),
"PASSWORD" VARCHAR2(40 BYTE),
"PHONE" NUMBER (20,0) NOT NULL ENABLE,
"LAST_LOGIN_DTS" DATE NOT NULL ENABLE,
CONSTRAINT "PK_USER_ID" PRIMARY KEY ("USER_ID")
);
